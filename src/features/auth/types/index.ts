export type AuthUser = {
  id: string;
  name: string;
};

export type LoginData = {
  id: string;
  name: string;
  password: string;
};

export type SignUpCredentials = {
  id: string;
  name: string;
  password: string;
};
